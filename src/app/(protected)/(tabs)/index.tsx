import React from "react";
import { StyleSheet, Text, View } from "react-native";

type Props = {};

const Home = (props: Props) => {
  return (
    <View>
      <Text className="text-center text-red-500 text-2xl">Home</Text>
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
